# サンプル資料（STEP 3 動作確認用）

これは知識ベースの**ローカル検証**用のプレースホルダです。
`POST /api/analyze` で `reindex_sources: true` を付けると、このファイルがチャンク化されベクトル INDEX に載ります。

- ポイント A: エージェントはコンテキスト内の記述のみを根拠に答える想定です。
- ポイント B: 実運用では論文・オープンデータの JSON などを `backend/data/`（`DATA_DIR`）に配置し、定期ジョブで API 経由取得するフローを STEP 4 以降で足せます。
