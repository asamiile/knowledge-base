# Local Docker Compose (matches db service credentials in docker-compose.yml)
DATABASE_URL=postgresql://knowledge:knowledge@db:5432/knowledge

# Optional: Supabase (STEP 2+) — replace with project connection string when used
# DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# STEP 3+
# GOOGLE_API_KEY=
